loglevel: info

inverters:
# EG4 6000xp
- enabled: true
  host: 192.168.5.103
  port: 8000
  serial: 3454660112
  datalog: BA32402393
  heartbeats: false
  publish_holdings_on_connect: false
  # Optional: Adjust read timeout (default 900s = 15 minutes)
  # read_timeout: 900

# EG4 18kPV (if you want to add it, uncomment and fill in)
# - enabled: true
#   host: 192.168.5.XXX
#   port: 8000
#   serial: XXXXXXXXXX
#   datalog: XXXXXXXXXX
#   heartbeats: false
#   publish_holdings_on_connect: false

databases:
- enabled: false
  url: postgres://lxp:lxp@localhost/lxp
  # url: mysql://lxp:lxp@localhost/lxp
  # url: sqlite://lxp.db

mqtt:
  enabled: true
  host: localhost
  port: 1883
  username:
  password:
  namespace: lxp
  homeassistant:
    enabled: true
    prefix: homeassistant
  # Optional: Publish individual input registers (creates more MQTT messages)
  # publish_individual_input: true

influx:
  enabled: false
  url: http://localhost:8086
  username:
  password:
  database: lxp

scheduler:
  enabled: false
  # Synchronize inverter time daily at midnight
  # timesync_cron: "0 0 * * *"
